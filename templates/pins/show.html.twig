{% extends 'base.html.twig' %}

{% block body %}
    {{ parent() }}
    <article>
        <h1>{{ pin.title }}</h1>
        <p>{{ pin.description }}</p>
    </article>

    <a href="{{ path('app_pins_edit',{id:pin.id}) }}">Edit</a> |
    <a href="{{ path('app_home') }}">Back</a>

{% endblock %}

{% block title %}
    {{ pin.title }}

{% endblock %}

