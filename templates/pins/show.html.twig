{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <h1>{{ pin.title }}</h1>
        <p>{{ pin.description }}</p>
    </article>

    <a href="{{ path('app_home') }}">Back</a>

{% endblock %}

{% block title %}
    {{ pin.title }}

{% endblock %}

